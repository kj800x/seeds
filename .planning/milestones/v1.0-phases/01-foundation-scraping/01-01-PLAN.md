---
phase: 01-foundation-scraping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - seeds-rs/Cargo.toml
  - seeds-rs/src/main.rs
  - seeds-rs/src/error.rs
  - seeds-rs/src/routes/mod.rs
  - seeds-rs/src/routes/home.rs
  - seeds-rs/src/routes/static_files.rs
  - seeds-rs/src/templates/mod.rs
  - seeds-rs/src/templates/layout.rs
  - seeds-rs/src/templates/home.rs
  - seeds-rs/src/db/mod.rs
  - seeds-rs/src/db/models.rs
  - seeds-rs/src/db/queries.rs
  - seeds-rs/migrations/001_initial.sql
  - seeds-rs/static/style.css
  - seeds-rs/static/htmx.min.js
autonomous: true
requirements:
  - INFR-01
  - INFR-02
  - INFR-03

must_haves:
  truths:
    - "App starts and serves a landing page at http://localhost:3000"
    - "Landing page shows dashboard shell with header, nav (Seeds active, Inventory/Schedule disabled), and main content area"
    - "SQLite database is created on first run with seeds and seed_images tables"
    - "Static CSS and HTMX JS files are served from /static/"
    - "App works fully offline once started (no CDN dependencies)"
  artifacts:
    - path: "seeds-rs/src/main.rs"
      provides: "Axum server startup, router assembly, state initialization"
      min_lines: 30
    - path: "seeds-rs/migrations/001_initial.sql"
      provides: "SQLite schema for seeds and seed_images tables"
      contains: "CREATE TABLE"
    - path: "seeds-rs/src/templates/layout.rs"
      provides: "Reusable page shell with header, nav, content area"
      exports: ["layout"]
    - path: "seeds-rs/static/style.css"
      provides: "Vanilla CSS with warm/earthy theme using custom properties"
      min_lines: 50
    - path: "seeds-rs/static/htmx.min.js"
      provides: "Local HTMX 2.0 for offline support"
      min_lines: 1
  key_links:
    - from: "seeds-rs/src/main.rs"
      to: "seeds-rs/src/routes/"
      via: "Router::new() with route handlers"
      pattern: "Router::new"
    - from: "seeds-rs/src/main.rs"
      to: "sqlx::SqlitePool"
      via: "SqlitePool::connect and sqlx::migrate!()"
      pattern: "SqlitePool::connect"
    - from: "seeds-rs/src/templates/layout.rs"
      to: "seeds-rs/static/style.css"
      via: "link rel stylesheet href /static/style.css"
      pattern: "/static/style.css"
---

<objective>
Set up the Axum server with SQLite, Maud layout shell, and static file serving -- the foundation everything else builds on.

Purpose: Establish the running app skeleton so scraper and UI plans have infrastructure to plug into.
Output: A bootable Rust binary serving a styled dashboard landing page with SQLite initialized.
</objective>

<execution_context>
@/home/kevin/.claude/get-shit-done/workflows/execute-plan.md
@/home/kevin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-scraping/01-CONTEXT.md
@.planning/phases/01-foundation-scraping/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project dependencies, SQLite schema, and data models</name>
  <files>seeds-rs/Cargo.toml, seeds-rs/migrations/001_initial.sql, seeds-rs/src/db/mod.rs, seeds-rs/src/db/models.rs, seeds-rs/src/db/queries.rs, seeds-rs/src/error.rs</files>
  <action>
1. Update seeds-rs/Cargo.toml with all dependencies from RESEARCH.md standard stack:
   - axum = "0.8"
   - maud = { version = "0.27", features = ["axum"] }
   - sqlx = { version = "0.8", features = ["runtime-tokio", "sqlite"] }
   - reqwest = { version = "0.13", features = ["json"] }
   - scraper = "0.22"
   - tokio = { version = "1", features = ["full"] }
   - tower-http = { version = "0.6", features = ["fs"] }
   - serde = { version = "1", features = ["derive"] }
   - serde_json = "1"
   - tracing = "0.1"
   - tracing-subscriber = "0.3"

2. Create seeds-rs/migrations/001_initial.sql with the schema from RESEARCH.md:
   - seeds table: id, product_handle (UNIQUE), source_url, title, description, category, subcategory, light_requirement, frost_tolerance, is_organic, is_heirloom, days_to_maturity, sow_depth, plant_spacing, germination_info, planting_instructions, growing_instructions, harvest_instructions, raw_html, shopify_product_id, tags_raw, created_at
   - seed_images table: id, seed_id (FK), shopify_image_id, position, original_url, local_filename, width, height, created_at
   - Use PRAGMA journal_mode=WAL at the top to avoid SQLite locking issues (see Pitfall 2 in RESEARCH.md)

3. Create seeds-rs/src/db/models.rs with Rust structs:
   - Seed struct with sqlx::FromRow derive, all growing fields as Option<String>
   - SeedImage struct with sqlx::FromRow derive
   - AppState struct (Clone) holding SqlitePool and PathBuf for data_dir

4. Create seeds-rs/src/db/queries.rs with stub functions (signatures only, bodies can return empty vecs or Ok(())):
   - list_seeds(pool) -> Vec<Seed>
   - get_seed(pool, id) -> Option<Seed>
   - get_seed_images(pool, seed_id) -> Vec<SeedImage>
   - insert_seed(pool, &NewSeed) -> i64
   - insert_image(pool, &NewSeedImage) -> ()
   - find_seed_by_handle(pool, handle) -> Option<Seed>
   These will be fully implemented -- they are straightforward sqlx queries using query_as! or query_as macros.

5. Create seeds-rs/src/db/mod.rs re-exporting models and queries.

6. Create seeds-rs/src/error.rs with an AppError enum (DbError, ScraperError, NotFound) that implements IntoResponse for Axum, returning appropriate HTML error pages.
  </action>
  <verify>
    <automated>cd /home/kevin/src/seeds-app/seeds-rs && cargo check 2>&1 | tail -5</automated>
  </verify>
  <done>Cargo.toml has all dependencies, migration SQL exists with both tables, Seed/SeedImage/AppState structs compile, DB query functions compile, AppError implements IntoResponse</done>
</task>

<task type="auto">
  <name>Task 2: Axum server, Maud layout, static files, and landing page</name>
  <files>seeds-rs/src/main.rs, seeds-rs/src/routes/mod.rs, seeds-rs/src/routes/home.rs, seeds-rs/src/routes/static_files.rs, seeds-rs/src/templates/mod.rs, seeds-rs/src/templates/layout.rs, seeds-rs/src/templates/home.rs, seeds-rs/static/style.css, seeds-rs/static/htmx.min.js</files>
  <action>
1. Download HTMX 2.0 minified JS to seeds-rs/static/htmx.min.js:
   - Use curl to fetch from https://unpkg.com/htmx.org@2.0.4/dist/htmx.min.js (or latest 2.0.x)
   - This local copy satisfies INFR-03 offline requirement

2. Create seeds-rs/static/style.css with warm/earthy vanilla CSS (per user decision):
   - CSS custom properties for theming: --color-earth-green, --color-warm-brown, --color-cream, --color-sage, etc.
   - Muted greens/browns palette, organic feel, soft borders (border-radius: 8px), gentle box-shadows
   - Dashboard shell: .app-header with logo and .main-nav, .content main area
   - Nav links: .nav-link.active styled, .nav-link.disabled grayed out with cursor: not-allowed
   - Seed list table/row styles: .seed-list, .seed-row with hover state
   - Responsive: single column on mobile, comfortable reading width on desktop
   - Typography: system font stack, comfortable line-height
   - Utility classes: .spinner (hidden by default, shown by HTMX indicator), .error-message, .form-result

3. Create seeds-rs/src/templates/layout.rs with a pub fn layout(title: &str, content: Markup) -> Markup function:
   - DOCTYPE, html lang="en", charset, viewport meta
   - Links /static/style.css and /static/htmx.min.js
   - header.app-header with h1 "Seeds" logo
   - nav.main-nav with: "Seeds" link (href="/", active), "Inventory" (span, disabled), "Schedule" (span, disabled)
   - main.content wrapping the content parameter
   - Follow Pattern 2 from RESEARCH.md exactly

4. Create seeds-rs/src/templates/home.rs with pub fn home_page(seeds: &[Seed]) -> Markup:
   - Uses layout() wrapper
   - Shows "Add a Seed" section at top with URL input form (hx-post="/seeds/add", hx-indicator=".spinner", hx-target=".form-result")
   - Input type="url", placeholder="Paste a Botanical Interests product URL...", required
   - Submit button "Add Seed", span.spinner "Adding seed..."
   - div.form-result for error messages
   - Below form: seed list section. If seeds empty, show friendly "No seeds yet" message
   - If seeds present, show compact list rows: each row is an anchor to /seeds/{id} showing name, variety (if any), category, days_to_maturity -- text-only, no thumbnails (per user decision)

5. Create seeds-rs/src/templates/mod.rs re-exporting layout and home.

6. Create seeds-rs/src/routes/home.rs with async fn home(State(state): State<AppState>) -> Markup handler that queries list_seeds and renders home_page.

7. Create seeds-rs/src/routes/static_files.rs -- this is optional if using nest_service directly in main.rs; skip if cleaner to configure in main.rs.

8. Create seeds-rs/src/routes/mod.rs re-exporting route handlers.

9. Update seeds-rs/src/main.rs following Pattern 1 from RESEARCH.md:
   - Initialize tracing_subscriber
   - Create SqlitePool with "sqlite:data/seeds.db?mode=rwc"
   - Run sqlx::migrate!()
   - Create data/images directory if not exists
   - Build AppState with pool and data_dir
   - Build Router with: "/" -> home, nest_service "/static" -> ServeDir::new("static"), nest_service "/images" -> ServeDir::new("data/images")
   - Bind to 0.0.0.0:3000, serve
   - Leave placeholder comments for seed routes (added in Plan 03)
  </action>
  <verify>
    <automated>cd /home/kevin/src/seeds-app/seeds-rs && cargo build 2>&1 | tail -5 && echo "BUILD OK"</automated>
  </verify>
  <done>Running `cargo run` starts the server on port 3000. Visiting http://localhost:3000 shows a styled dashboard with header, nav (Seeds active, Inventory/Schedule disabled), add-seed form, and empty seed list. /static/style.css and /static/htmx.min.js are served correctly. SQLite database is created at data/seeds.db on first run.</done>
</task>

</tasks>

<verification>
1. `cd seeds-rs && cargo build` compiles without errors
2. `cargo run` starts server, http://localhost:3000 returns HTML with dashboard layout
3. `curl http://localhost:3000/static/style.css` returns CSS content
4. `curl http://localhost:3000/static/htmx.min.js` returns HTMX JS
5. `ls data/seeds.db` confirms SQLite database was created
6. `sqlite3 data/seeds.db ".tables"` shows seeds and seed_images tables
</verification>

<success_criteria>
- Axum server starts and serves landing page at localhost:3000
- Dashboard shell has header with "Seeds" logo, nav with Seeds/Inventory/Schedule (last two disabled)
- Warm/earthy CSS theme applied (muted greens/browns, soft borders, gentle shadows)
- SQLite database created with seeds and seed_images tables
- HTMX served locally (not from CDN)
- Add-seed form visible (not yet functional -- wired in Plan 03)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-scraping/01-01-SUMMARY.md`
</output>
