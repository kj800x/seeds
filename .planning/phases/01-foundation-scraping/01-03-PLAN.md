---
phase: 01-foundation-scraping
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - seeds-rs/src/main.rs
  - seeds-rs/src/routes/mod.rs
  - seeds-rs/src/routes/seeds.rs
  - seeds-rs/src/templates/mod.rs
  - seeds-rs/src/templates/seed_detail.rs
  - seeds-rs/src/templates/home.rs
  - seeds-rs/static/style.css
autonomous: false
requirements:
  - SCRP-01
  - SCRP-04

must_haves:
  truths:
    - "User can paste a BI product URL into the form and submit it"
    - "While scraping, user sees a loading indicator ('Adding seed...')"
    - "On success, user is redirected to the new seed's detail page"
    - "On error, user sees an inline error message and can retry"
    - "Duplicate URLs show 'This seed is already in your collection' with link to existing entry"
    - "Seed list on home page shows compact text-only rows with name, variety, category, days to maturity"
    - "Clicking a seed row navigates to /seeds/{id} detail page"
    - "Detail page shows hero image at top, then structured summary sections, then collapsible original scraped text"
    - "Product images are served from /images/{seed_id}/ paths"
  artifacts:
    - path: "seeds-rs/src/routes/seeds.rs"
      provides: "Add seed POST handler, seed detail GET handler"
      exports: ["add_seed", "seed_detail"]
      min_lines: 50
    - path: "seeds-rs/src/templates/seed_detail.rs"
      provides: "Seed detail page with hero image and structured sections"
      exports: ["seed_detail_page"]
      min_lines: 60
  key_links:
    - from: "seeds-rs/src/routes/seeds.rs"
      to: "seeds-rs/src/scraper/mod.rs"
      via: "calls scrape_and_save from POST handler"
      pattern: "scrape_and_save"
    - from: "seeds-rs/src/routes/seeds.rs"
      to: "seeds-rs/src/db/queries.rs"
      via: "calls get_seed and get_seed_images for detail page"
      pattern: "get_seed.*get_seed_images"
    - from: "seeds-rs/src/templates/seed_detail.rs"
      to: "/images/{seed_id}/"
      via: "img src pointing to locally served images"
      pattern: "/images/"
    - from: "seeds-rs/src/main.rs"
      to: "seeds-rs/src/routes/seeds.rs"
      via: "Router route registration for /seeds/{id} and /seeds/add"
      pattern: "route.*seeds"
---

<objective>
Wire the scraper into the UI: add seed form submission, seed list display, and seed detail page -- completing the full user-facing flow.

Purpose: Connect all the infrastructure (Plan 01) and scraping logic (Plan 02) into a working user experience where seeds can be added, listed, and viewed.
Output: A fully functional app where a user can paste a BI URL, see it scraped, view it in the list, and click through to a detail page.
</objective>

<execution_context>
@/home/kevin/.claude/get-shit-done/workflows/execute-plan.md
@/home/kevin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-scraping/01-CONTEXT.md
@.planning/phases/01-foundation-scraping/01-RESEARCH.md
@.planning/phases/01-foundation-scraping/01-01-SUMMARY.md
@.planning/phases/01-foundation-scraping/01-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 and 02 -- executor needs these -->

From seeds-rs/src/db/models.rs:
- AppState { db: SqlitePool, data_dir: PathBuf }
- Seed struct (all fields)
- SeedImage struct (all fields)

From seeds-rs/src/db/queries.rs:
- list_seeds(pool) -> Result<Vec<Seed>>
- get_seed(pool, id) -> Result<Option<Seed>>
- get_seed_images(pool, seed_id) -> Result<Vec<SeedImage>>

From seeds-rs/src/scraper/mod.rs:
- scrape_and_save(state, url) -> Result<i64, AppError>
- AppError should have a Duplicate variant carrying the existing seed_id

From seeds-rs/src/templates/layout.rs:
- layout(title, content) -> Markup

From seeds-rs/src/templates/home.rs:
- home_page(seeds) -> Markup (has the add-seed form with hx-post="/seeds/add")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed routes, detail template, and router wiring</name>
  <files>seeds-rs/src/routes/seeds.rs, seeds-rs/src/routes/mod.rs, seeds-rs/src/templates/seed_detail.rs, seeds-rs/src/templates/mod.rs, seeds-rs/src/main.rs, seeds-rs/static/style.css</files>
  <action>
1. Create seeds-rs/src/routes/seeds.rs with two handlers:

   a. pub async fn seed_detail(State(state): State<AppState>, Path(id): Path<i64>) -> Result<Markup, AppError>
      - Call get_seed(pool, id) -- return 404 AppError if None
      - Call get_seed_images(pool, seed_id)
      - Render seed_detail_page(seed, images)

   b. pub async fn add_seed(State(state): State<AppState>, Form(input): Form<AddSeedInput>) -> impl IntoResponse
      - AddSeedInput struct with url: String field
      - Validate URL starts with "https://www.botanicalinterests.com/products/" (or http variant) -- if not, return inline HTML error: "Not a valid Botanical Interests product URL"
      - Call scrape_and_save(&state, &input.url).await
      - On success (returns seed_id): return HX-Redirect header to /seeds/{seed_id} (HTMX will follow it)
      - On duplicate error: return inline HTML "This seed is already in your collection" with an anchor link to /seeds/{existing_id}
      - On other error: return inline HTML "Could not extract seed data — please try again"
      - All error responses are HTML fragments targeting .form-result div (HTMX swap)

2. Create seeds-rs/src/templates/seed_detail.rs with pub fn seed_detail_page(seed: &Seed, images: &[SeedImage]) -> Markup:
   - Uses layout() wrapper with seed title
   - Back link at top: a href="/" "Back to Seeds"
   - Hero image section: if images has an entry with position == 1, show large img with src="/images/{seed_id}/{filename}" -- if no images, show a placeholder or skip
   - h1 with seed title
   - Structured summary sections (per user decision -- "structured summary sections below"):
     * "Growing Info" section: days_to_maturity, light_requirement, frost_tolerance (show label + value pairs, skip if None)
     * "Planting" section: sow_depth, plant_spacing, germination_info, planting_instructions
     * "Harvest" section: harvest_instructions
     * "About" section: category, subcategory, is_organic badge, is_heirloom badge, description (rendered from body_html)
   - Collapsible "Original Scraped Text" section below (per user decision -- "both structured summary AND collapsible original"):
     * Use HTML details/summary element for collapsible
     * Show raw growing_instructions, planting_instructions, harvest_instructions as original text blocks
   - Each section should only render if it has content (don't show empty sections)

3. Update seeds-rs/src/templates/mod.rs to export seed_detail.

4. Update seeds-rs/src/routes/mod.rs to export seeds routes.

5. Update seeds-rs/src/main.rs router to add:
   - .route("/seeds/{id}", get(seed_detail))
   - .route("/seeds/add", post(add_seed))
   - Use axum 0.8 path syntax /{id} NOT /:id (Pitfall 1)

6. Update seeds-rs/src/templates/home.rs if needed -- ensure the seed list rows link to /seeds/{id} and display: name (bold), category, days_to_maturity in a compact row. Ensure the hx-post on the form targets .form-result and uses hx-indicator=".spinner".

7. Update seeds-rs/static/style.css with additional styles for:
   - .seed-detail page: hero image (max-width: 100%, centered), section headers, label-value pairs
   - .back-link styling
   - .badge for organic/heirloom indicators
   - details/summary collapsible styling (warm/earthy consistent)
   - .error-message red-tinted styling for form errors
   - .duplicate-message with link styling
  </action>
  <verify>
    <automated>cd /home/kevin/src/seeds-app/seeds-rs && cargo build 2>&1 | tail -5 && echo "BUILD OK"</automated>
  </verify>
  <done>App compiles and runs. Full user flow works: paste URL in form -> see spinner -> redirected to detail page showing hero image, structured sections, collapsible original text. Returning to home shows seed in compact list row. Duplicate URLs show inline message with link. Invalid URLs show inline error.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify full scraping flow end-to-end</name>
  <what-built>Complete seed scraping app: Axum server with SQLite, BI product scraper (dual JSON/HTML), dashboard UI with seed list, detail pages, and add-seed form with HTMX.</what-built>
  <how-to-verify>
    1. Start the app: cd seeds-rs && cargo run
    2. Open http://localhost:3000 in browser
    3. Verify dashboard layout: header with "Seeds" logo, nav with Seeds active and Inventory/Schedule disabled, warm/earthy styling
    4. Paste a Botanical Interests product URL (e.g., https://www.botanicalinterests.com/products/tomato-pole-cherry-sun-gold) into the form and click "Add Seed"
    5. Verify: spinner shows briefly, then you're redirected to a detail page
    6. On detail page verify: hero image at top, structured growing info sections below, collapsible original text section
    7. Click "Back to Seeds" -- verify the seed appears in the list with name, category, days to maturity
    8. Try adding the same URL again -- verify you see "This seed is already in your collection" with a link
    9. Try an invalid URL -- verify you see "Not a valid Botanical Interests product URL"
    10. Try adding a second different seed to verify the list grows
    11. Stop the server, restart it (cargo run), verify seeds persist (SQLite)
    12. Check data/images/ directory has downloaded product images
  </how-to-verify>
  <resume-signal>Type "approved" if the flow works, or describe any issues with scraping, display, or styling</resume-signal>
</task>

</tasks>

<verification>
1. `cargo build` succeeds
2. Full flow: paste URL -> scrape -> redirect to detail -> back to list
3. Detail page shows hero image, structured sections, collapsible original text
4. Error states work: invalid URL, duplicate URL
5. Seeds persist across server restart
6. Images downloaded and served from /images/
</verification>

<success_criteria>
- User can paste a BI URL and the app scrapes, stores, and displays the seed data
- Seed list shows compact text-only rows (name, category, days to maturity)
- Detail page has hero image, structured sections (Growing, Planting, Harvest, About), collapsible original text
- Duplicate detection works with link to existing entry
- Invalid URL shows inline error
- Images served from local filesystem
- Data persists across restarts
- Human has verified the end-to-end flow
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-scraping/01-03-SUMMARY.md`
</output>
